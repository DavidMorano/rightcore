DQ

This is a hack to get the status information from the S program to display on
the backing terminal when multiple virtual terminals are overlaid on the backing
terminal (see the SCREEN program for more information on virtual terminals).

Synopsis:
$ dq

