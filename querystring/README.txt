QUERYSTRING

This is a KSH shell built-in command.  It processes a "query-string" that is
either supplied by argument or an environment variable, and it then prints out
key-value pairs from that query-string.

Synopsis:
$ querystring [-qs <querystring>] [-V]

Arguments:
-qs <querystring>	query-string
-V			print command version to standard-error and then exit

