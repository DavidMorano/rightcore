COPYCD

This program will traverse a directory hierarchy of a CDROM and copy everything
(with certain translations) to someplace (designated) on your hard disk.

Example:
$ copycd dir1 dir2 

would copy everything under 'dir1' and put it into 'dir2'.

