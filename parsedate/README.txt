PARSEDATE

This is a little date conversion daemon. This was intended for use when only the
UNIX 'getdate(3)' function was used for date conversions but since this
subroutine is so incredibly inept at converting even the simplest dates, it (and
this daemon) have been abandoned!

