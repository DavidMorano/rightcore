WEBCOUNTS

This little program is actually used as daemon to server as front-end
to the WEBCOUNTER program.  This program essentially prints out all of
the counters associated with a particular database-name.  This program
takes one arhument: the name of the data-base desired.

Synopsis:

# webcounts <db>

where

<db>		is the data-base name desired

