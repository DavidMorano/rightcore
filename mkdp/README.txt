MKDP (Make Data Present)

This little program calculates a decoding table for use in calculating
data-present bits for memory operations. The decoding table entries are each
16-bits wide to accommodate 8-byte operands along with an error indication.
Zero-sized operands are indicated by all zeros.

