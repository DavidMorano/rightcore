ISUSER

This tiny program just determines if the name given as the first argument
is a valid user on the current system or not.

Synopsis:

$ isuser <name>

where:

<name>		is the username to check for

Returns:

0		if the specified username exits on the current system
!=0		if the specified username does not exit on the current system


