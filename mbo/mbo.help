MBO

This just reads a mailbox under file-record-lock and copies it to
standard output.  It truncates the mailbox after a successful copy-out
operation.

Synopsis :

$ mailboxout [mailbox] > outfile


