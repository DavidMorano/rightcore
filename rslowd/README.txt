RSLOWD

This is the RSLOW Daemon program. This program sits and waits on a directory to
receive new jobs. When a job comes in, it reads the job and determines how to
execute it if possible. Jobs are automatically removed from the directory when
they are finished running.

