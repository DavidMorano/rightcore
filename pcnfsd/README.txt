PCNFSD

This is Dave Morano's private version of a PCNFS daemon for UNIX.
This version implements Version 1 (only really) of the PCNFS protocol.

Since most PCs are using (or require) version two of the protocol,
this daemon is not too much use anymore.

