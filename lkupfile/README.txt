LKUPFILE

This program accesses a file of key-value pairs and prints to standard output
the value of the specified keys.

Synopsis:
$ lkupfile [-db <dbfile>] <key(s)>

Arguments:
-db <dbfile>	file of key-values to reference
<key(s)>	keys to lookup the values of (prints one value per line)

