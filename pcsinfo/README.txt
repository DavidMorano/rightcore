PCSINFO

This program prints out information from various PCS databases.  This program
is also used as a live (real program) test of the MAILALIAS and related objects
used for mail address handling.

Synopsis:
$ pcscinfo [<ema(a)>] -V

Arguments:
<ema(s)>	email address(es)
-V		print program version to standard-error and then exit

