DISCHAR

This program reads characters on the standard input and prints them out
on standard output in HEX form.  The program finishes when acarriage
return is entered.

Synopsis:

$ dischar [-V]

where:

-V		print program version to standard-error and then exit

