PATHADD

This is a KSH function. It adds additional file-paths to a path-type shell
variables.

Synopsis:
$ pathadd <name> <path(s)> [-b|-f]

Arguments:
<name>		name of variable to modify
<path(s)>	path(s) to add to the variable
-f		put new path at the front
-b		put new path at the back (default)

