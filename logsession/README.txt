LOGSESSION

This program prints out the session field of the log-in record for the
current process session (if the process session was ever logged-in in
the first place).

This program is essentially equivalent to the USERINFO program when used as:

Synopsis:
$ userinfo - logsession

