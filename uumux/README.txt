UUMUX

This is a program that implements the client side of a UUMUX network
interaction. This program basically just calls the UUMUX subroutine interface to
do the detailed functions needed to transfer data over a UUMUX network link.

Synopsis:
$ uumux [-n] <node>!<service> [<service_argument(s)> ...]

