UTMPMAINT

This program performs some (simple) maintenance on the UTMPX database.

Synopsis:

$ utmpmaint [-l|-m] [<name(s)>] [-db <dbfile>] [-V] 

where:

-l		list mode
-m		maintenance mode
<name(s)>	restrict listing to only these name(s)
-db <dbfile>	alternative UTMPX file
-V		print program version to standard-error and then exit

