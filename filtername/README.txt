FILTERNAME

This program reads a list of filenames in on its standard input. It then writes
the file names it finds except for the names that were specifled as invocation
arguments.

Synopsis:
$ filtername [-V]

