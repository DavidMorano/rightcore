HOMEPAGE

This program makes a HOMEPAGE document in the HTM language. The HTTP header
is also outputted.

Synopsis:
$ homepage [-d[=<runint>]] [-svcs <svc(s)>] [-V]

Arguments:
-d[=<runint>]	run in daemon mode w/ optional run interval
-svcs <svc(s)>	services to include
-V		print command version to standard-error and then exit

