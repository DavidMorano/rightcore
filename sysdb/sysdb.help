SYSDB

This command queries one of several system DBs.

Synopsis:
$ sysdb <dbname> [<query(s)> [-r] |-a] [-V]

Arguments:
<dbname>	name of the specific DB to query; one of:
			passwd, shadow, group, project, userattr, shells 
			protocols, networks, hosts, ipnodes, services 
<query(s)>	key to query
-r		perform reverse query for those DBs that support it
-a		retrieve all accessible DB entries
-V		print command version to standard-error and then exit

