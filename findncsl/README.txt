FINDNCSL

Find the number of commented and non-commented source (code) lines in C-language
type files. The default is to perform the function (collectively) on the current
working directory (descending the directory tree as necessary).

Synopsis:
$ findncsl [<file(s)>]

Arguments:
<file(s)>	file(s) to process

