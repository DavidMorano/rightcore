RMAILERD

This program processes an RMAILER type mail message. This is a mail message
encapsulated in a way that includes the message recipients within a single input
data stream.

Synopsis:
$ rmailerd [{conf|-} [port]] [-C conf] [-p port] [-?v] [-D[=n]]

Arguments:
conf		configuation file
port		optional port to bind to for incoming jobs

