TESTMKDIRS

This program creates directories. It also creates any missing components in the
path the specified directories.

Synopsis:
$ testmkdirs [<dir(s)>] [-V]

Arguments:
<dir(s)>	optional directories to create within the user-tmp directory
-V		print program version to standard-error and then exit

