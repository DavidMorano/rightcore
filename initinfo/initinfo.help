INIINFO

This is a KSH shell built-in command.  It prints out INITINFO entries.  If a
queried entry does not exist an empty string is printed.

Synopsis:
$ initinfo [<queries>] [-af <afile>] [-of <ofile>] [-V]

Arguments:
<queries>	query request(s) for one or more INITINFO variables
-af <afile>	argument list file
-of <ofile>	alternative output file
-V		print command version to standard-error and then exit

