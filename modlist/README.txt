MODLIST

This program will list all of the STREAMS modules on the devices listed as
invocation arguments. Note that this program is similar (really similar) to the
'strconf' program that is shipped with Sun Solaris 2.6 UNIX, except that this
program was probably written first.

Synopsis:
$ modlist

