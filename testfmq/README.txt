TESTFMQ

This program provides an example of using File-Message-Queues (FMQ) as an
interprocess (or interthread) communication channel from a client to a server.

= reader (start first)
$ testfmq.x <mq> reader

= writer
$ testfmq.x <mq>

