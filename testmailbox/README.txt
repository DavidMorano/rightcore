TESTMAILBOX

This little program tests the MAILBOX object.  It essentially just
treats a file (specified as an argument) to be a mailbox and it tries
to open and read it.  Information about the messages is then printed
out on STDOUT.

Synopsis:

$ testmailbox.x new.mb




