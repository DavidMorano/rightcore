LEVOCHECKOUT

This directory contains the source code to the Levo 'checkout' program.

Synopsis:
$ checkout levo_o dbx_o -cont -e

for normal batch-mode operation.  The program will eventually
exit on its own (needed for batch-mode).

