OPENWWATHER

These files together create the |openweather(3dam)| subroutine. This subroutine
returns a file-descriptor to the current (or most current) weather information
for a given weather station (passed as a parameter).

