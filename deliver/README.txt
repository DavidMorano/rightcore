DELIVER

This program delivers the mail message on standard-input to the specified
mailboxes of one or more recipients who are users on the current system. Users
are identified by their usernames. If no mailbox is specified, the default
mailbox "new" is used.

Synopsis:
$ deliver [<user(s)>] [-f <from>] [-m <mailbox>] [-M <mailer>]

