TPS (for Darwin)

This tiny program just prints out processes (in some format). This program is
written for the Darwin OS. It will not run (nor even compile) on any other OS.

Synopsis:
$ tps [-u <username>] [-s <sid>] [-p <pid>] [-pg <pgid>] [-a] [-e] [-f]

