DISCHARBITS

This program reads characters on the standard input and prints them out on
standard output in a form suitable for inclusion into a C language program as an
array of bits. If run interactively, the program completes when a carriage
return is entered on the standard input.

