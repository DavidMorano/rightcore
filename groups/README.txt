GROUPS

This is a version of the standard |groups(1)| program for those systems which
do not have that program.

Synopsis:
$ groups [<user>]

Arguments:
<user>		optional other user (default is current user) to lookup

