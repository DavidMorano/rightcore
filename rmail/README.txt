DMAIL

This is a mail delivery program similar to '/bin/mail -d'. It should be as close
to it as possible so that programs like '/usr/lib/sendmail' can call it as if
they had called '/bin/usr/mail -d'!

